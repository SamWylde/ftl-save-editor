<UserControl x:Class="FtlSaveEditor.Views.CargoEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <StackPanel Margin="0,0,0,20">
        <!-- Header -->
        <DockPanel Margin="0,0,0,16">
            <TextBlock Text="Cargo Editor" FontSize="22" FontWeight="Bold"
                       Foreground="{StaticResource AccentBlueBrush}" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="AddCargoBtn" Content="+ Add Cargo" Style="{StaticResource DarkButton}"/>
            </StackPanel>
        </DockPanel>

        <TextBlock Text="Manage cargo items stored in your ship. Type the item blueprint ID for each entry."
                   Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,12"/>

        <!-- Cargo list container -->
        <Border Background="{StaticResource BgMediumBrush}" BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1" CornerRadius="6" Padding="16">
            <StackPanel>
                <!-- Column header -->
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="40"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="70"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="#" Grid.Column="0" FontWeight="SemiBold" FontSize="11"
                               Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock Text="Cargo ID" Grid.Column="1" FontWeight="SemiBold" FontSize="11"
                               Foreground="{StaticResource AccentBlueBrush}"/>
                    <TextBlock Text="Remove" Grid.Column="2" FontWeight="SemiBold" FontSize="11"
                               Foreground="{StaticResource AccentRedBrush}" TextAlignment="Center"/>
                </Grid>

                <Separator Background="{StaticResource BorderBrush}" Margin="0,0,0,8"/>

                <StackPanel x:Name="CargoListPanel"/>

                <!-- Empty state -->
                <TextBlock x:Name="EmptyText" Text="No cargo items"
                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="13"
                           Margin="0,4,0,4" Visibility="Collapsed"/>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
